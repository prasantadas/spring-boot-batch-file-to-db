spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/my_db?useSSL=false
    password: root
    username: root
  batch:
    job:
      enabled: false

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: debug
